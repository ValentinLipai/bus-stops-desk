import { createStore } from 'vuex'
import { TimeTableStoreModule } from "@/store/modules/timetable";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    TimeTableStoreModule
  }
})
